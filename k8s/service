apiVersion: v1
kind: Service
metadata:
  name: counter-app-service  
  namespace: default         
spec:
  selector:
    app: counter-app         
  ports:
    - protocol: TCP          
      port: 5000             
      targetPort: 5000       
  type: ClusterIP            
